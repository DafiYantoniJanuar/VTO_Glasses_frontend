services:
  # React Frontend Service
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vto_frontend
    restart: unless-stopped
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
